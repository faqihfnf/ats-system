// Batasan upload dokumen pendukung kandidat.
// Dipakai bersama oleh validasi server (action.documents.ts) & client (dialog upload).
export const MAX_DOC_SIZE_MB = 5;
export const MAX_DOCS_PER_CANDIDATE = 10;
